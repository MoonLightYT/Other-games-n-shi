local Library = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()

local Window = Library:CreateWindow({
    Title = "Fluent " .. Library.Version,
    SubTitle = "by dawid",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 460),
    Acrylic = true, -- Set to false to disable blur
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl -- Used when there's no MinimizeKeybind
})

-- Define tabs
local Tabs = {
    Main = Window:AddTab({ Title = "Ultra Shake", Icon = "0" }),
}

-- Adding button to the Main tab
Tabs.Main:AddButton({
    Title = "Auto Ultra Shake",
    Description = "Very important button",
    Callback = function()
        local player = game.Players.LocalPlayer
        local ultraShakeInBackpack = player.Backpack:FindFirstChild("Ultra Shake")
        local character = player.Character or player.CharacterAdded:Wait() -- Wait for character if not loaded
        local ultraShakeInCharacter = character:FindFirstChild("Ultra Shake")

        if ultraShakeInBackpack then
            print("Found Ultra Shake in Backpack!")
            player.Character.Humanoid:EquipTool(ultraShakeInBackpack)
        elseif ultraShakeInCharacter then
            print("Found Ultra Shake in Character!")
            local function autoConsumeBoosts()
                while true do
                    wait(1) -- Adjust the wait time as needed
                    for _, item in pairs(player.Backpack:GetChildren()) do
                        if item.Name == "Ultra Shake" then -- Check if the item's name matches
                            player.Character.Humanoid:EquipTool(item)
                            item:Activate()
                        end
                    end
                end
            end
            autoConsumeBoosts() -- Start consuming boosts
        else
            print("Ultra Shake not found")
        end
    end
})
